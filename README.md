## Fundraising dApp Contracts

First, install the NPM modules:

```bash
npm install
```

To compile:

```bash
node compile.js
```

To deploy:
Add private key and Infura API key to deploy.js

```bash
node deploy.js
```
